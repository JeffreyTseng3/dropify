import { connect } from "react-redux";
import MusicPlayerBar from "./music_player_bar";


const msp = state => {
    return {

    };
};

const mdp = dispatch => {
    return {
    };
};

export default connect(msp, mdp)(MusicPlayerBar);