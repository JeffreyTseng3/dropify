import { connect } from "react-redux";
import NavBar from "./navbar";


const msp = state => {

    return {

    };
};

const mdp = dispatch => {
    return {

    };
};

export default connect(msp, mdp)(NavBar);