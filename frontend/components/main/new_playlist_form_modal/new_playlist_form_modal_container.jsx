import { connect } from 'react-redux';
import React from 'react';
import NewPlaylistFormModal from "./new_playlist_form_modal";

const msp = state => { 
    return {

    };
};

const mdp = dispatch => {
    return {

    };
};

export default connect(msp, mdp)(NewPlaylistFormModal);